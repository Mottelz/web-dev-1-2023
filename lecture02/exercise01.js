// Create a variable called abc with an integer value
let abc = 10;

// Print your name abc times
for(let i = 0; i < abc; i++) {
    console.log("Mottel");
};

// Create a function that takes in two strings and returns them as one  string
const contactStrings = (a, b) => {
    return `${a} ${b}`;
};

// Create a variable whose value can't be changed
const a = 'unchangeable';
